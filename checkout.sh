git checkout -b v0.1.x-alpha

cd src/components
git checkout -b v0.1.x-alpha

cd ../engine
git checkout -b v0.1.x-alpha

cd ../views/blueprints
git checkout -b v0.1.x-alpha

cd ../files
git checkout -b v0.1.x-alpha

