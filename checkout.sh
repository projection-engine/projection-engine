cd docs
git checkout -b v1.0.x-alpha

cd ../src/components
git checkout -b v1.0.x-alpha

cd ../project/engine
git checkout -b v1.0.x-alpha

cd ../components/blueprints
git checkout -b v1.0.x-alpha

cd ../files
git checkout -b v1.0.x-alpha

cd ../../../../
git checkout -b v1.0.x-alpha

sleep 5s